{
  "name": "rift26qc",
  "version": "1.0.0",
  "description": "RIFT26QC project",
  "scripts": {
    "build": "mkdir -p dist && cp -r public/* dist/",
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "vercel": "^50.20.0"
  }
}